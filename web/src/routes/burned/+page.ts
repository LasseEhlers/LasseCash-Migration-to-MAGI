/**
 * Client-only: the whole page is an audit the VISITOR performs. Rendering it
 * on the server would mean asking them to trust our arithmetic, which is the
 * opposite of the point.
 */
export const ssr = false;
