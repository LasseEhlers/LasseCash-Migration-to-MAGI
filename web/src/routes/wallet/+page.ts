/**
 * Client-only: every figure here is this account's own money, read from the
 * chain in the browser through the engine. There is nothing a server could
 * usefully render and nothing a crawler should index.
 */
export const ssr = false;
