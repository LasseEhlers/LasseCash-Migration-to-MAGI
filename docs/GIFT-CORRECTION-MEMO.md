# A correction, for the record

*Draft for Lasse. Suggested mechanism: send this as a memo attached to a
small LASSECASH transfer to `null` on Hive-Engine — same layer the original
mass letter was sent on, and it puts the correction permanently on-chain,
next to the run it corrects, the same way everything else in this project
is meant to be provable rather than just stated. A token amount like
9.00292595 (matching the original gift, so it's recognizably the same
series) is one option; any positive amount works.*

*Date note: 2026-08-23, roughly 01:11–02:50 CEST — the window between the
gift list being built and the last recorded send before the run was
stopped. That's from local file timestamps, not a verified on-chain read,
so treat it as approximate if precision matters later.*

---

A correction, for the record.

In the letter I sent with the mass gift on 2026-08-23, I wrote that I had
made a script that reaches all active users on Hive. That wasn't true when
I sent it, and I want it corrected honestly rather than left as written.

What actually happened: I built the script and started it. Of the 4,443
accounts I intended to reach, **1,510** actually received the gift and the
letter before I stopped the run, to put my full time into finishing the
MAGI migration instead.

So: about a third of who I meant to reach, got it. That's the true number,
and this is where it's recorded.

**The plan — not a promise.** When the migration is live and I have more
time and more LASSECASH to give away again, I intend to finish what I
started: reach all active Hive users, the same way, with the same kind of
letter, but for the state at that future time. No date on it. I'm not
going to write one just to have written one. But it's the plan, and now
it's on the record too.

— Lasse Ehlers
